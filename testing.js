// const { checkInventory } = require("./checkInventory");
// checkInventory({ bookId: "2", quantity: 1})

const { restorePoints } = require("./restorePoints");
restorePoints({userId :"1", pointsRedeemed: 1, total: 0})